# 🏗️ Authentication Architecture

## 📐 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ROTANERA AUTH SYSTEM                        │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐   │
│  │  Login Page     │  │  Register Page  │  │  Protected Page │   │
│  │  /login         │  │  /register      │  │  /dashboard     │   │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘   │
│           │                    │                     │             │
│           └────────────────────┼─────────────────────┘             │
│                                │                                   │
│  ┌─────────────────────────────▼─────────────────────────────┐    │
│  │           AUTH COMPONENTS & HOOKS                         │    │
│  ├───────────────────────────────────────────────────────────┤    │
│  │  • SignInForm          • useAuth()                        │    │
│  │  • SignUpForm          • useAuthContext()                 │    │
│  │  • OAuthButtons        • useRequireAuth()                 │    │
│  │  • AuthProvider                                           │    │
│  └─────────────────────────────┬─────────────────────────────┘    │
│                                │                                   │
└────────────────────────────────┼───────────────────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   Supabase Client       │
                    │   (Browser)             │
                    │   src/lib/supabase/     │
                    │   client.ts             │
                    └────────────┬────────────┘
                                 │
┌────────────────────────────────┼───────────────────────────────────┐
│                         MIDDLEWARE LAYER                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │               middleware.ts (Root)                           │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  1. updateSession() - Refresh Supabase session         │  │  │
│  │  │  2. isAuthenticated() - Check auth cookies             │  │  │
│  │  │  3. handleAuthRedirect() - Route protection            │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │         middleware-helpers.ts                                │  │
│  │  • Route config (protected, auth, public)                   │  │
│  │  • Authentication check logic                               │  │
│  │  • Redirect logic                                           │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└────────────────────────────────┬───────────────────────────────────┘
                                 │
┌────────────────────────────────┼───────────────────────────────────┐
│                          API LAYER                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐   │
│  │  POST           │  │  POST           │  │  POST/GET       │   │
│  │  /auth/signup   │  │  /auth/login    │  │  /auth/signout  │   │
│  │                 │  │                 │  │                 │   │
│  │  • Validate     │  │  • Validate     │  │  • Clear        │   │
│  │  • Create user  │  │  • Verify       │  │    session      │   │
│  │  • Send email   │  │  • Set cookies  │  │  • Redirect     │   │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘   │
│           │                    │                     │             │
│           └────────────────────┼─────────────────────┘             │
│                                │                                   │
│  ┌─────────────────────────────▼─────────────────────────────┐    │
│  │              Supabase Server Client                       │    │
│  │              src/lib/supabase/server.ts                   │    │
│  │  • createServerSupabaseClient()                           │    │
│  │  • createServerSupabaseAdmin()                            │    │
│  │  • getCurrentUser()                                       │    │
│  │  • getCurrentSession()                                    │    │
│  └─────────────────────────────┬─────────────────────────────┘    │
│                                │                                   │
└────────────────────────────────┼───────────────────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   OAuth Callback        │
                    │   /auth/callback        │
                    │  • Exchange code        │
                    │  • Establish session    │
                    └────────────┬────────────┘
                                 │
┌────────────────────────────────┼───────────────────────────────────┐
│                        SUPABASE LAYER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Supabase Auth                             │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  • User Management                                     │  │  │
│  │  │  • Session Management                                  │  │  │
│  │  │  • Token Generation & Refresh                          │  │  │
│  │  │  • Password Hashing                                    │  │  │
│  │  │  • OAuth Integration                                   │  │  │
│  │  │  • Email Verification                                  │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL Database (Supabase)                  │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  auth.users table (managed by Supabase)                │  │  │
│  │  │  • id, email, encrypted_password, metadata, etc.       │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  public.users table (your custom data)                 │  │  │
│  │  │  • id, name, email, avatar, auth_id (FK), etc.         │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      EXTERNAL SERVICES                              │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐    │
│  │  Google OAuth   │  │  Email Service  │  │  (Future: More) │    │
│  │  Provider       │  │  (Supabase)     │  │  OAuth Providers│    │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Authentication Flows

### 1. Email/Password Registration Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Fills signup form
     ▼
┌─────────────────┐
│  SignUpForm     │
│  Component      │
└────┬────────────┘
     │
     │ 2. Client-side validation
     │    • Name, email, password
     │    • Password strength
     ▼
┌─────────────────┐
│  POST           │
│  /auth/signup   │
└────┬────────────┘
     │
     │ 3. Server-side validation
     │    • Validate inputs
     │    • Check existing user
     ▼
┌─────────────────┐
│  Supabase       │
│  auth.signUp()  │
└────┬────────────┘
     │
     │ 4. Create auth user
     │    • Hash password
     │    • Store in auth.users
     ▼
┌─────────────────┐
│  Email Service  │
│  (Optional)     │
└────┬────────────┘
     │
     │ 5. Send verification email
     │    (if enabled)
     ▼
┌─────────────────┐
│  User clicks    │
│  verify link    │
└────┬────────────┘
     │
     │ 6. Redirect to callback
     ▼
┌─────────────────┐
│  /auth/callback │
│  Exchange code  │
└────┬────────────┘
     │
     │ 7. Establish session
     │    • Set cookies
     │    • Create JWT tokens
     ▼
┌─────────────────┐
│  Dashboard      │
│  (Protected)    │
└─────────────────┘
```

---

### 2. Email/Password Login Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Enters credentials
     ▼
┌─────────────────────┐
│  SignInForm         │
│  Component          │
└────┬────────────────┘
     │
     │ 2. Client validation
     │    • Email format
     │    • Required fields
     ▼
┌─────────────────────┐
│  POST               │
│  /auth/login        │
└────┬────────────────┘
     │
     │ 3. Server validation
     ▼
┌─────────────────────┐
│  Supabase           │
│  signInWithPassword │
└────┬────────────────┘
     │
     │ 4. Verify credentials
     │    • Check password hash
     │    • Check email verified
     ▼
┌─────────────────────┐
│  Generate Session   │
│  • Access token     │
│  • Refresh token    │
└────┬────────────────┘
     │
     │ 5. Set HTTP-only cookies
     │    • sb-access-token
     │    • sb-refresh-token
     ▼
┌─────────────────────┐
│  Return success     │
│  + user data        │
└────┬────────────────┘
     │
     │ 6. Client receives response
     │    • Show success toast
     │    • Redirect to app
     ▼
┌─────────────────────┐
│  Dashboard          │
│  (Protected)        │
└─────────────────────┘
```

---

### 3. Google OAuth Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Clicks "Login with Google"
     ▼
┌─────────────────────────┐
│  OAuthButtons           │
│  Component              │
└────┬────────────────────┘
     │
     │ 2. Trigger OAuth
     │    supabase.auth.signInWithOAuth()
     ▼
┌─────────────────────────┐
│  Redirect to Google     │
│  Consent Screen         │
└────┬────────────────────┘
     │
     │ 3. User approves
     │    • Select account
     │    • Grant permissions
     ▼
┌─────────────────────────┐
│  Google redirects back  │
│  to /auth/callback      │
│  with code=xxx          │
└────┬────────────────────┘
     │
     │ 4. Exchange code for session
     ▼
┌─────────────────────────┐
│  Supabase               │
│  exchangeCodeForSession │
└────┬────────────────────┘
     │
     │ 5. Create/Update user
     │    • Check if user exists
     │    • Store OAuth data
     │    • Link to auth.users
     ▼
┌─────────────────────────┐
│  Generate Session       │
│  • JWT tokens           │
│  • Set cookies          │
└────┬────────────────────┘
     │
     │ 6. Redirect to app
     ▼
┌─────────────────────────┐
│  Dashboard              │
│  (Protected)            │
└─────────────────────────┘
```

---

### 4. Route Protection Flow (Middleware)

```
┌─────────────────┐
│  User Visits    │
│  /dashboard     │
└────┬────────────┘
     │
     │ 1. Request intercepted
     ▼
┌─────────────────────────┐
│  middleware.ts          │
│  (Root Level)           │
└────┬────────────────────┘
     │
     │ 2. Call updateSession()
     ▼
┌─────────────────────────┐
│  Refresh Supabase       │
│  session if needed      │
└────┬────────────────────┘
     │
     │ 3. Check authentication
     │    isAuthenticated(request)
     ▼
┌─────────────────────────┐
│  Check cookies          │
│  • sb-access-token      │
│  • sb-refresh-token     │
└────┬────────────────────┘
     │
     ├─── NO (Not authenticated)
     │    │
     │    │ 4a. Check if route is protected
     │    ▼
     │    ┌─────────────────────────┐
     │    │  Is /dashboard in       │
     │    │  protected routes?      │
     │    └────┬────────────────────┘
     │         │ YES
     │         │ 5a. Redirect to login
     │         ▼
     │    ┌─────────────────────────┐
     │    │  /login?redirect=       │
     │    │  /dashboard             │
     │    └─────────────────────────┘
     │
     └─── YES (Authenticated)
          │
          │ 4b. Check if route is auth page
          ▼
          ┌─────────────────────────┐
          │  Is /login or /register │
          │  in auth routes?        │
          └────┬────────────────────┘
               │ YES
               │ 5b. Redirect to app
               ▼
          ┌─────────────────────────┐
          │  /app/test              │
          │  (defaultAuthRedirect)  │
          └─────────────────────────┘
               │ NO
               │ 6. Allow access
               ▼
          ┌─────────────────────────┐
          │  Render page            │
          └─────────────────────────┘
```

---

### 5. Session Management Flow

```
┌─────────────────────────────────────────────────────────┐
│  User logged in, browsing app                           │
└────┬────────────────────────────────────────────────────┘
     │
     │ Every page request
     ▼
┌─────────────────────────┐
│  Middleware checks      │
│  access token expiry    │
└────┬────────────────────┘
     │
     ├─── Token valid
     │    │
     │    │ Continue request
     │    ▼
     │    ┌─────────────────────┐
     │    │  Render page        │
     │    └─────────────────────┘
     │
     └─── Token expired
          │
          │ Use refresh token
          ▼
     ┌─────────────────────────┐
     │  Supabase auto-refresh  │
     │  • Generate new tokens  │
     │  • Update cookies       │
     └────┬────────────────────┘
          │
          ├─── Refresh successful
          │    │
          │    │ Continue request
          │    ▼
          │    ┌─────────────────┐
          │    │  Render page    │
          │    └─────────────────┘
          │
          └─── Refresh failed
               │
               │ Session expired
               ▼
          ┌─────────────────────┐
          │  Redirect to login  │
          └─────────────────────┘
```

---

### 6. Logout Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Clicks logout button
     ▼
┌─────────────────┐
│  signOut()      │
│  from useAuth   │
└────┬────────────┘
     │
     │ 2. Call API
     ▼
┌─────────────────┐
│  POST           │
│  /auth/signout  │
└────┬────────────┘
     │
     │ 3. Server processing
     ▼
┌─────────────────┐
│  Supabase       │
│  auth.signOut() │
└────┬────────────┘
     │
     │ 4. Clear session
     │    • Invalidate tokens
     │    • Delete cookies
     ▼
┌─────────────────┐
│  Return success │
└────┬────────────┘
     │
     │ 5. Client cleanup
     │    • Clear local state
     │    • Clear context
     ▼
┌─────────────────┐
│  Redirect to /  │
│  (home page)    │
└─────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Layer 1: CLIENT-SIDE VALIDATION                           │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  • Email format validation                            │ │
│  │  • Password strength rules                            │ │
│  │  • Required field checks                              │ │
│  │  • Real-time feedback                                 │ │
│  └───────────────────────────────────────────────────────┘ │
│                         │                                   │
│                         ▼                                   │
│  Layer 2: SERVER-SIDE VALIDATION                           │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  • Re-validate all inputs                             │ │
│  │  • Sanitize data                                      │ │
│  │  • Check duplicate emails                             │ │
│  │  • Verify request origin                              │ │
│  └───────────────────────────────────────────────────────┘ │
│                         │                                   │
│                         ▼                                   │
│  Layer 3: SUPABASE AUTH SECURITY                           │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  • Password hashing (bcrypt)                          │ │
│  │  • PKCE flow for OAuth                                │ │
│  │  • JWT token signing                                  │ │
│  │  • Rate limiting                                      │ │
│  │  • CSRF protection                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                         │                                   │
│                         ▼                                   │
│  Layer 4: SESSION SECURITY                                 │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  • HTTP-only cookies                                  │ │
│  │  • Secure flag (HTTPS)                                │ │
│  │  • SameSite attribute                                 │ │
│  │  • Auto token refresh                                 │ │
│  │  • Session expiry                                     │ │
│  └───────────────────────────────────────────────────────┘ │
│                         │                                   │
│                         ▼                                   │
│  Layer 5: DATABASE SECURITY                                │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  • Row Level Security (RLS)                           │ │
│  │  • SQL injection prevention                           │ │
│  │  • Encrypted at rest                                  │ │
│  │  • Access control policies                            │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 Component Relationships

```
┌──────────────────────────────────────────────────────────────┐
│                     Component Tree                           │
└──────────────────────────────────────────────────────────────┘

RootLayout
│
├── AuthProvider (Context)
│   │
│   ├── LoginPage
│   │   ├── SignInForm
│   │   │   ├── Input (name, email, password)
│   │   │   ├── Button (submit)
│   │   │   └── useAuth hook
│   │   └── OAuthButtons
│   │       └── Google button
│   │
│   ├── RegisterPage
│   │   ├── SignUpForm
│   │   │   ├── Input (name, email, password, confirm)
│   │   │   ├── Button (submit)
│   │   │   └── useAuth hook
│   │   └── OAuthButtons
│   │       └── Google button
│   │
│   └── ProtectedPage
│       ├── useAuth hook
│       ├── useAuthContext hook
│       └── getCurrentUser (server)
│
└── Toaster (notifications)

Hooks:
├── useAuth()
│   ├── Manages user state
│   ├── Manages session state
│   ├── Provides signOut()
│   └── Listens to auth changes
│
├── useAuthContext()
│   └── Consumes AuthProvider context
│
└── useRequireAuth()
    └── Redirects if not authenticated
```

---

## 🗄️ Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   DATA FLOW DIAGRAM                         │
└─────────────────────────────────────────────────────────────┘

User Input (Form)
      │
      │ 1. Form submission
      ▼
Client State (React)
      │
      │ 2. Client validation
      ▼
API Request (fetch)
      │
      │ 3. POST to /auth/*
      ▼
Route Handler (Next.js)
      │
      │ 4. Server validation
      ▼
Supabase Client (Server)
      │
      │ 5. Auth operation
      ▼
Supabase Auth Service
      │
      │ 6. Process request
      ▼
PostgreSQL Database
      │
      │ 7. Store/retrieve data
      ▼
Response (JSON)
      │
      │ 8. Return to client
      ▼
Update Client State
      │
      │ 9. Update UI
      ▼
Redirect/Feedback
```

---

## 🔄 State Management

```
┌─────────────────────────────────────────────────────────────┐
│                  STATE MANAGEMENT                           │
└─────────────────────────────────────────────────────────────┘

Global State (AuthProvider Context)
├── user: User | null
├── session: Session | null
├── loading: boolean
└── methods: { signOut, refreshUser }

Local State (Component)
├── formData: { email, password, ... }
├── errors: { field: string }
├── isLoading: boolean
├── showPassword: boolean
└── showConfirmPassword: boolean

Server State (Supabase)
├── auth.users (authentication)
├── public.users (profile data)
└── auth.sessions (active sessions)

Cookie State (Browser)
├── sb-access-token (JWT)
├── sb-refresh-token (JWT)
└── sb-provider-token (OAuth)
```

---

## 🎯 Request/Response Cycle

```
┌─────────────────────────────────────────────────────────────┐
│            REQUEST/RESPONSE CYCLE                           │
└─────────────────────────────────────────────────────────────┘

1. Browser Request
   └─> GET /dashboard

2. Middleware Intercepts
   ├─> updateSession() - Refresh tokens if needed
   ├─> isAuthenticated() - Check cookies
   └─> handleAuthRedirect() - Apply protection rules

3. Decision Point
   ├─> Authenticated?
   │   ├─> YES: Continue to handler
   │   └─> NO: Redirect to /login
   │
   └─> Protected route?
       ├─> YES: Require auth
       └─> NO: Allow public access

4. Route Handler (if allowed)
   ├─> Server Component: getCurrentUser()
   └─> API Route: createServerSupabaseClient()

5. Supabase Query
   ├─> Verify session
   ├─> Fetch user data
   └─> Apply RLS policies

6. Response Generation
   ├─> Render HTML (Server Component)
   └─> Return JSON (API Route)

7. Client Update
   ├─> Update DOM
   ├─> Update React state
   └─> Show feedback (toast)
```

---

## 📊 File Dependencies

```
middleware.ts
  ├─> lib/supabase/middleware.ts (updateSession)
  └─> lib/auth/middleware-helpers.ts (isAuthenticated, handleAuthRedirect)

lib/supabase/client.ts
  └─> @supabase/ssr (createBrowserClient)

lib/supabase/server.ts
  ├─> @supabase/ssr (createServerClient)
  └─> next/headers (cookies)

lib/supabase/middleware.ts
  ├─> @supabase/ssr (createServerClient)
  └─> next/server (NextRequest, NextResponse)

hooks/use-auth.tsx
  ├─> lib/supabase/client.ts (createClient)
  ├─> @supabase/supabase-js (User, Session)
  └─> next/navigation (useRouter)

components/auth-page/sign-in-form.tsx
  ├─> components/ui/input
  ├─> components/ui/button
  ├─> sonner (toast)
  └─> next/navigation (useRouter)

components/auth-page/oauth-buttons.tsx
  ├─> lib/supabase/client.ts (createClient)
  └─> sonner (toast)

app/(api)/auth/*/route.ts
  ├─> lib/supabase/server.ts (createServerSupabaseClient)
  └─> next/server (NextResponse)
```

---

## 🔐 Token Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│                   TOKEN LIFECYCLE                           │
└─────────────────────────────────────────────────────────────┘

1. User logs in
   │
   ▼
2. Supabase generates tokens
   ├─> Access Token (JWT) - expires in 1 hour
   └─> Refresh Token (JWT) - expires in 30 days
   │
   ▼
3. Tokens stored in HTTP-only cookies
   ├─> sb-access-token
   └─> sb-refresh-token
   │
   ▼
4. User makes authenticated request
   │
   ▼
5. Middleware checks access token
   │
   ├─> Valid? Continue request
   │
   └─> Expired?
       │
       ▼
       Use refresh token
       │
       ├─> Valid? Generate new access token
       │
       └─> Expired? Redirect to login
       │
       ▼
6. Access token renewed automatically
   │
   ▼
7. User continues browsing
   (cycle repeats every hour)
```

---

**Last Updated:** 2024  
**Version:** 1.0  
**Status:** Production Ready ✅